<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head'); %>
</head>
<body class="container">
    <header>
        <%- include('../partials/header'); %>
    </header>

    <main>
        <div class="jumbotron">
            <h1>Arvin Studio</h1>
            <p>Selamat Datang, sahabat plugin audio.</p>
        </div>

        <div class="row">

            <% posts.forEach(function(post) { %>
            <div class="col-sm-6 mb-3">
                <div class="card">
                    <div class="card-body">
                    <h5 class="card-title"><%= post.title %></h5>
                    <p class="card-text"><%= post.content.slice(0, 50)+'...' %></p>
                    <a href="/read/<%= post.slug_id %>" class="btn btn-primary">Lanjut Membaca</a>
                    </div>
                </div>
            </div>
            <% }); %>

        </div>

    </main>

    <footer>
        <%- include('../partials/footer'); %>
    </footer>

</body>
</html>